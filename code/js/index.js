const screenWidth = window.screen.width;
const screenHeight = window.screen.height;
const minWidth = 1920;
const minHeight = 1080;

if (screenWidth >= minWidth && screenHeight >= minHeight) {window.location = "code/html/main.html"}